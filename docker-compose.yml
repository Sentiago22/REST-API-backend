version: "3.9"

services:
  app:
    build: .
    container_name: search_app
    depends_on:
      - redis
    ports:
      - "8080:8080"

  redis:
    image: redis:alpine
    container_name: redis
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data

volumes:
  redis-data:
